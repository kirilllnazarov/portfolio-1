import styled from "styled-components";

const GoTopBtn = styled.button`
	background-color: #1f1f20;
	padding: 8px;
	position: fixed;
	right: 30px;
	bottom: 30px;
`;

export const S = {
	GoTopBtn,
};
